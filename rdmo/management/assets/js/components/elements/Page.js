import React, { Component} from 'react'
import PropTypes from 'prop-types'

const Page = ({ page }) => {
  return null
}

Page.propTypes = {
  page: PropTypes.object.isRequired
}

export default Page
